import { View } from 'react-native';

/** Center tab uses custom tabButton to open CheckInFlow; this screen is never focused long. */
export function CheckInPlaceholderScreen() {
  return <View style={{ flex: 1 }} />;
}
